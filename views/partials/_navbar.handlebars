<nav class="navbar navbar-expand-lg navbar-dark bg-dark" style="border-bottom: 2px solid rgb(138, 138, 138);">
    <a class="navbar-brand" href="/">Maga Checkin</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            {{#if user}}
            <li class="nav-item active">
                <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
            </li>
            <li>
                <a class="nav-link" href="/admin">Gestão
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/admin/viewAll">Histórico</a>
            </li>
        </ul>
        <div class="my-2 my-lg-0">
            <div class="username-navbar inline mr-2" id="userName"></div>
            <a class="btn btn-info mr-sm-2" href="/users/logout">Sair</a>
        </div>
        {{else}}
        <li class="nav-item">
            <a class="nav-link" href="/users/login">Entrar</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="/users/register">Cadastrar-se</a>
        </li>
        {{/if}}
        </ul>
    </div>
</nav>

{{#if user.name}}
<script>
    let nomeDoUsuario = "{{ user.name }}"
    let arrayNome = nomeDoUsuario.split(" ")
    let campoAPreencher = document.getElementById("userName")
    campoAPreencher.innerText = arrayNome[0]
</script>
{{/if}}